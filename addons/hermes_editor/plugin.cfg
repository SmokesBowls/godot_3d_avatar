[plugin]

name="Hermes Editor"
description="A real Hermes agent seated inside the Godot editor. No operation allowlist, no JSON command interpreter — Hermes keeps its own native file/shell/search/test tools, exactly as it has outside the editor. First milestone only: prove Hermes genuinely sits inside the editor and can inspect/edit the current project. Wiring this into EngAIn's shared continuity authority (:8767) is a deliberate, separate next step."
author="EngAIn"
version="0.1.0"
script="plugin.gd"
